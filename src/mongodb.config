%%% -*- mode:erlang -*-
%%% Warning - this config file *must* end with <dot><whitespace>

%% write log files to sasl_dir
[
 {sasl,
  [
   {sasl_error_logger, false},
	 {error_logger_mf_dir, "/tmp/otp_error_logs/"},
	 {error_logger_mf_maxbytes, 10485760},
	 {error_logger_mf_maxfiles, 10},
	 {utc_log, true}
  ]}

%% Add app specifig configs below here. 
%% *Note* Don't forget to put a comma after the sasl config if you do. 

].
